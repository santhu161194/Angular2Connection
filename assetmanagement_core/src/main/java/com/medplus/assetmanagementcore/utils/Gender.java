package com.medplus.assetmanagementcore.utils;




public enum Gender {
	f,m};
	/*MALE("male"), // schema pos, toStore mandatory
	FEMALE("female");// schema pos, supplierId mandatory
	

	private String value;

	// Reverse-lookup map for getting a day from an abbreviation
	private static final Map<String,Employee> lookup = new HashMap<>();

	static {
		for (Employee emp: Employee.) {
			lookup.put(emp.getValue(),emp);
		}
	}

	OptivalInvoiceType(String v) {
		value = v;
	}

	public String getValue() {
		return value;
	}

	public static OptivalInvoiceType getName(String invoiceType) {
		return lookup.get(invoiceType);
	}
}
};*/
